import { ListLocksResponse } from './ListLocks';

export class CheckLockResponse extends ListLocksResponse {
  /**
   * It's literally the same response as ListLocks
   * but only the target lock
   */
}
